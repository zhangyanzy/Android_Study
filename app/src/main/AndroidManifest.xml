<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tarenwang.android_study">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".ToggleButton.ToggleButtonActivity" />
        <activity android:name=".Marquee.MarqueeTextViewActivity" />
        <activity android:name=".RadioButton.RadioButtonActivity"></activity>
        <activity android:name=".spinner.SpinnerActivity">

        </activity>
        <activity android:name=".GridView.GridViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>